# ============================================================
# CI 测试环境配置（不依赖 Nacos）
# ============================================================
spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
